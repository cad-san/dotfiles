" GUI Setting
if has('gui_macvim')
    set showtabline=2	" タブを常に表示
    set imdisable	" IMを無効化
    set transparency=0	" 透明度を指定
    set antialias
    set guifont=Ricty:h14
    colorscheme wombat
endif

